// Atividade
// Crie uma função para verificar se um número
// É positivo ou negativo

function comparar(a) {
    if (a > 0) {
        console.log('Positivo');
    } else if (a < 0) { 
        console.log('negativo');
    } else {
        console.log('Zero');
    }
}
comparar(5); 

// const verificar = (numero) =>
// Operação ternária
    //numero > 0 ?"Positivo":"Negativo"
    //resultado = verificar (10)
    //console.log(resultado)
    const media = somaDasNotas / media.length
console.log(`A média geral da turma é: ${media}`)
